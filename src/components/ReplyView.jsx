import ReplyHelper from './ReplyHelper'

export default function ReplyView({ reply, replyOptions, language }) {
  return <ReplyHelper reply={reply} replyOptions={replyOptions} language={language} />
}
